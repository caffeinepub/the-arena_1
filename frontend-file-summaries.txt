{
  "frontend/src/index.css": "Global styles implementing the Arena dark theme with OKLCH color tokens, Outfit/Bebas Neue fonts, neon gold accent colors, glow utility classes, and card hover effects.",
  "frontend/tailwind.config.js": "Extended Tailwind config with arena-specific color tokens (arena-dark, arena-neon, arena-surface), custom font families (Outfit, Bebas Neue), neon box-shadow utilities, and pulse-neon animation.",
  "frontend/src/App.tsx": "Main app component with TanStack Router; added the `/messages` route (with `partner` search param validation) wired to `MessagesPage` alongside the existing Feed, Upload, ContentDetail, UserProfile, and Mind routes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; added `useGetFollowersList` hook that fetches the list of follower Principals for a given user via `actor.getFollowers()` (public query, no auth required), and updated `useFollowUser`/`useUnfollowUser` to also invalidate the `followersList` cache on success.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header and nav links; fixed the `badge` type error by defining a typed `NavLink` interface and building the nav links array imperatively (pushing the Messages link conditionally), so `badge` is always typed as `number | undefined` instead of `unknown`.",
  "frontend/src/components/LoginButton.tsx": "Internet Identity login/logout button with loading state, query cache clearing on logout, and arena-themed styling.",
  "frontend/src/pages/ProfileSetupModal.tsx": "First-login modal that collects an artist/creator name, optional profile picture, and optional bio before saving the profile; now passes `counts: { followers: 0n, following: 0n }` in the initial profile object to satisfy the updated `UserProfile` type.",
  "frontend/src/components/ContentCard.tsx": "Responsive content card component updated to use `content.mimeType` string (replacing the removed `FileType` enum) for badge labels, thumbnail fallback selection, and image error handling, with helper functions `isAudioMime`/`isVideoMime`/`getMimeLabel` derived from MIME type strings.",
  "frontend/src/components/MediaPlayer.tsx": "Custom HTML5 audio/video player with separate audioRef/videoRef, full playback controls (play/pause, seek, skip ±15s, volume, mute, repeat, next/prev), and graceful error handling that displays user-friendly fallback messages (with AlertCircle icon) for unsupported formats, decode errors, and network failures.",
  "frontend/src/components/FileUploadInput.tsx": "Reusable drag-and-drop file input component that now accepts all file formats (accept=\"*\") with no size restrictions, displays file type icons, size, and MIME type, with updated hint text listing all supported formats per category.",
  "frontend/src/utils/videoFrameCapture.ts": "Utility function that captures the current frame of an HTMLVideoElement as a PNG Blob using canvas.toBlob().",
  "frontend/src/components/ThumbnailCreator.tsx": "Thumbnail creation component that shows video frame capture (with scrubber) for video files or an image upload field for audio files, with preview display and clear functionality.",
  "frontend/src/pages/UploadPage.tsx": "Upload form page fixed to conditionally render `ThumbnailCreator` only when `mediaFile` is non-null (resolving the `File | null` type error), and uses `onThumbnailCapture` prop name matching `ThumbnailCreator`'s interface.",
  "frontend/src/pages/FeedPage.tsx": "Discovery feed page updated to filter Music/Video tabs using `isAudioMime`/`isVideoMime` helpers on `content.mimeType` instead of the removed `FileType` enum.",
  "frontend/src/pages/ContentDetailPage.tsx": "Content detail page that fetches and displays a single piece of content with media player, action buttons, stats, and metadata; now resolves the uploader's artist profile name via `useGetUserProfile` and displays it (with skeleton loading state and fallback to shortened principal) as a clickable link navigating to the artist's profile page.",
  "frontend/src/components/QueuePanel.tsx": "Slide-out queue panel updated to use `content.mimeType` string instead of the removed `FileType` enum, with `isAudioMime` and `getMimeLabel` helpers for badge display and thumbnail fallback logic.",
  "frontend/src/components/CommentsSection.tsx": "Comments section component that fetches and displays comments for a given contentId, with a comment input form for authenticated users, author name resolution, delete-own-comment support (now gated behind a ConfirmationDialog with destructive styling), loading/error states, and arena-themed styling.",
  "frontend/src/components/ConfirmationDialog.tsx": "Reusable AlertDialog-based confirmation component accepting title, description, confirm/cancel labels, onConfirm callback, isPending state, and a destructive flag for styling the confirm button as either destructive red or arena-neon.",
  "frontend/src/components/ProfileMenu.tsx": "Dropdown menu for authenticated users showing a circular avatar (with initials fallback) and profile actions; fixes the `Uint8Array<ArrayBufferLike>` TypeScript error by wrapping `pictureBytes` in `new Uint8Array(...)` before passing to the `Blob` constructor.",
  "frontend/src/components/EditProfileDialog.tsx": "Dialog for editing the authenticated user's display name, bio, and profile picture; now includes the required `counts` field (preserved from the existing profile, defaulting to `{ followers: 0n, following: 0n }`) when calling `editProfile.mutateAsync` to satisfy the updated `UserProfile` type.",
  "frontend/src/components/UserProfileView.tsx": "Reusable profile view component; the follower count is now a clickable button that opens the `FollowersModal`, with local `isFollowersModalOpen` state controlling visibility, while all existing follow/unfollow/message functionality is preserved.",
  "frontend/src/pages/UserProfilePage.tsx": "Page component for the `/profile/:principal` route that parses the principal from URL params and renders `UserProfileView`, with an error state for invalid principals.",
  "frontend/src/pages/WhatsOnYourMindPage.tsx": "\"What's on Your Mind\" page with post composer and feed; each post card now has a collapsible comments panel toggled by a `MessageCircle` button showing the comment count, with `ThoughtCommentItem` for individual comments (with owner-only delete + confirmation dialog), `CommentsPanel` for the full inline section (input form for authenticated users, login prompt for guests), and all comment operations wired to the new thought-comment hooks.",
  "frontend/src/components/ConversationView.tsx": "Conversation detail component displaying a full message thread between the authenticated user and a partner; fixed the type error by removing the extraneous `partnerStr` property from the `markAsRead.mutate` call, keeping only the valid `conversationKey` and `messageIndex` fields.",
  "frontend/src/pages/MessagesPage.tsx": "Messages page with two-panel layout (conversation list + conversation detail); fixed the `navigate` search type error by passing `{ partner: undefined }` instead of `{}` when clearing the partner query param on mobile back navigation.",
  "frontend/src/components/FollowersModal.tsx": "New modal component that displays a scrollable list of followers for a given user Principal; uses `useGetFollowersList` to fetch follower principals, renders each as a `FollowerItem` sub-component (with avatar, display name resolved via `useGetUserProfile`/`useGetProfilePicture`, and a clickable row that navigates to the follower's profile page), accessible to all visitors including unauthenticated users."
}