{
  "frontend/src/index.css": "Global styles implementing the Arena dark theme with OKLCH color tokens, Outfit/Bebas Neue fonts, neon gold accent colors, glow utility classes, and card hover effects.",
  "frontend/tailwind.config.js": "Extended Tailwind config with arena-specific color tokens (arena-dark, arena-neon, arena-surface), custom font families (Outfit, Bebas Neue), neon box-shadow utilities, and pulse-neon animation.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup for Feed (/), Upload (/upload), and ContentDetail (/content/:id) routes, wrapped in a Layout shell with profile setup modal logic.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; fixed `useDeleteCallerUserProfile` to call the correct `actor.deleteUserProfile()` method, added `useDeleteProfile` hook, added `useEditProfile` mutation hook that calls `actor.editProfile`, and ensured `useDeleteContent` invalidates `contentBySearch` query key in addition to existing keys.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header, nav links, queue icon button with badge, and conditional rendering of ProfileMenu (when authenticated) or LoginButton (when not), plus mobile nav, footer with caffeine.ai attribution, and QueuePanel.",
  "frontend/src/components/LoginButton.tsx": "Internet Identity login/logout button with loading state, query cache clearing on logout, and arena-themed styling.",
  "frontend/src/pages/ProfileSetupModal.tsx": "First-login modal that blocks app access until the user sets an artist/creator name, using the saveCallerUserProfile mutation.",
  "frontend/src/components/ContentCard.tsx": "Responsive content card with thumbnail, file type badge, view count, and action buttons (queue, like, comments); now includes a hover-revealed delete button visible only to the content owner, with a confirmation dialog before deletion.",
  "frontend/src/components/MediaPlayer.tsx": "Custom HTML5 audio/video player refactored to use separate `audioRef` and `videoRef` refs (instead of a single shared ref), with `PlayerControls` extracted as a proper top-level component (fixing the inner-function-component remount bug), and all event handlers stabilized with `useCallback`. Supports full playback controls (play/pause, seek, skip ±15s, volume, mute, repeat, next track) for both audio and video content types.",
  "frontend/src/components/FileUploadInput.tsx": "Reusable drag-and-drop file input component supporting audio, video, image, and combined 'media' categories with file type icons, size display, and clear button styled with the arena dark theme.",
  "frontend/src/utils/videoFrameCapture.ts": "Utility function that captures the current frame of an HTMLVideoElement as a PNG Blob using canvas.toBlob().",
  "frontend/src/components/ThumbnailCreator.tsx": "Thumbnail creation component that shows video frame capture (with scrubber) for video files or an image upload field for audio files, with preview display and clear functionality.",
  "frontend/src/pages/UploadPage.tsx": "Upload form page for audio/video content; fixed the `Uint8Array<ArrayBufferLike>` type error by storing the thumbnail as a plain `ArrayBuffer` and constructing a fresh `new Uint8Array(thumbnailBuffer)` (typed as `Uint8Array<ArrayBuffer>`) before passing to `ExternalBlob.fromBytes`.",
  "frontend/src/pages/FeedPage.tsx": "Discovery feed page with hero banner, All/Music/Video filter tabs, search, recent/popular sort, and responsive content grid; uses the new `useGetUserProfile` hook (imported from `useQueries`) to resolve uploader names per card.",
  "frontend/src/pages/ContentDetailPage.tsx": "Content detail page with media player, action buttons (Add to Queue, Like, Comments toggle), and a \"Delete Content\" button visible only to the content owner that opens a confirmation dialog and navigates back to the feed on successful deletion.",
  "frontend/src/components/QueuePanel.tsx": "Slide-out right drawer panel displaying the user's playback queue with thumbnails, file type badges, remove/reorder (up/down) buttons, clear-all action, currently-playing highlight with neon glow, and click-to-navigate functionality — styled with the arena dark/neon theme.",
  "frontend/src/components/CommentsSection.tsx": "New component that fetches and displays comments for a given contentId, with a comment input form for authenticated users, author name resolution, delete-own-comment support, loading/error states, and arena-themed styling.",
  "frontend/src/components/ConfirmationDialog.tsx": "Reusable AlertDialog-based confirmation component accepting title, description, confirm/cancel labels, onConfirm callback, isPending state, and a destructive flag for styling the confirm button as either destructive red or arena-neon.",
  "frontend/src/components/ProfileMenu.tsx": "Dropdown menu for authenticated users showing their display name with \"Edit Profile\" (opens EditProfileDialog), \"Log Out\", and \"Delete Account\" (opens ConfirmationDialog) options; uses the corrected `useDeleteProfile` hook and clears the query cache and session on account deletion or logout.",
  "frontend/src/components/EditProfileDialog.tsx": "New dialog component for editing the authenticated user's display name; pre-populates the input with the current profile name, calls `useEditProfile` on submit, shows a loading spinner during the mutation, and closes on success with a toast notification."
}